- name: 安裝五大 AI CLI (codex / claude-code / opencode / gemini / copilot)
  hosts: all
  gather_facts: no

  vars:
    node_version: "20"
    target_user: "cary_yu"

  tasks:
    - name: 主機資訊
      raw: echo "$(hostname) / $(hostname -I | awk '{print $1}') / $(. /etc/os-release && echo $PRETTY_NAME)"
      register: hostinfo
      changed_when: false

    - debug: msg="{{ hostinfo.stdout | trim }}"

    - name: 安裝系統相依套件 (curl/ca-certs/gnupg/git/build-essential)
      raw: echo "{{ ansible_ssh_pass }}" | sudo -S DEBIAN_FRONTEND=noninteractive apt-get install -y curl ca-certificates gnupg git build-essential

    - name: 加入 NodeSource {{ node_version }}.x 來源並安裝 Node.js
      raw: |
        if command -v node >/dev/null && node -v | grep -q '^v{{ node_version }}\.'; then
          echo "Node already installed: $(node -v)"
        else
          curl -fsSL https://deb.nodesource.com/setup_{{ node_version }}.x -o /tmp/nodesource_setup.sh
          echo "{{ ansible_ssh_pass }}" | sudo -S bash /tmp/nodesource_setup.sh
          echo "{{ ansible_ssh_pass }}" | sudo -S DEBIAN_FRONTEND=noninteractive apt-get install -y nodejs
        fi
      register: node_out

    - name: 設定 {{ target_user }} 的 npm global prefix → ~/.npm-global (避免 EACCES)
      raw: |
        echo "{{ ansible_ssh_pass }}" | sudo -S -u {{ target_user }} -H bash -lc '
          mkdir -p $HOME/.npm-global
          npm config set prefix $HOME/.npm-global
          grep -q "npm-global/bin" $HOME/.bashrc 2>/dev/null || echo "export PATH=\$HOME/.npm-global/bin:\$PATH" >> $HOME/.bashrc
          echo "prefix=$(npm config get prefix)"
        '
      register: prefix_out
      changed_when: false

    # ─── 每套 CLI 獨立 task：Semaphore 會逐 task 即時輸出 ───────────────
    - name: "[1/5] npm install -g @openai/codex"
      raw: |
        echo "=== START codex $(date +%T) ==="
        echo "{{ ansible_ssh_pass }}" | sudo -S -u {{ target_user }} -H bash -lc '
          export PATH=$HOME/.npm-global/bin:$PATH
          timeout 300 npm install -g --no-fund --no-audit @openai/codex 2>&1 | tail -5
        '
        echo "=== END   codex $(date +%T) rc=$? ==="
      ignore_errors: yes

    - name: "[2/5] npm install -g @anthropic-ai/claude-code"
      raw: |
        echo "=== START claude $(date +%T) ==="
        echo "{{ ansible_ssh_pass }}" | sudo -S -u {{ target_user }} -H bash -lc '
          export PATH=$HOME/.npm-global/bin:$PATH
          timeout 300 npm install -g --no-fund --no-audit @anthropic-ai/claude-code 2>&1 | tail -5
        '
        echo "=== END   claude $(date +%T) rc=$? ==="
      ignore_errors: yes

    - name: "[3/5] npm install -g opencode-ai"
      raw: |
        echo "=== START opencode $(date +%T) ==="
        echo "{{ ansible_ssh_pass }}" | sudo -S -u {{ target_user }} -H bash -lc '
          export PATH=$HOME/.npm-global/bin:$PATH
          timeout 300 npm install -g --no-fund --no-audit opencode-ai 2>&1 | tail -5
        '
        echo "=== END   opencode $(date +%T) rc=$? ==="
      ignore_errors: yes

    - name: "[4/5] npm install -g @google/gemini-cli"
      raw: |
        echo "=== START gemini $(date +%T) ==="
        echo "{{ ansible_ssh_pass }}" | sudo -S -u {{ target_user }} -H bash -lc '
          export PATH=$HOME/.npm-global/bin:$PATH
          timeout 300 npm install -g --no-fund --no-audit @google/gemini-cli 2>&1 | tail -5
        '
        echo "=== END   gemini $(date +%T) rc=$? ==="
      ignore_errors: yes

    - name: "[5/5] npm install -g @github/copilot"
      raw: |
        echo "=== START copilot $(date +%T) ==="
        echo "{{ ansible_ssh_pass }}" | sudo -S -u {{ target_user }} -H bash -lc '
          export PATH=$HOME/.npm-global/bin:$PATH
          timeout 300 npm install -g --no-fund --no-audit @github/copilot 2>&1 | tail -5
        '
        echo "=== END   copilot $(date +%T) rc=$? ==="
      ignore_errors: yes

    - name: 安裝 Hermes Agent (per-user)  # 暫時停用：install.sh 在 worker 會 hang，TODO 手動安裝
      raw: echo "[skipped] hermes install — TODO manual"
      changed_when: false

    - name: 驗證各 CLI 版本
      raw: |
        echo "{{ ansible_ssh_pass }}" | sudo -S -u {{ target_user }} -H bash -lc '
          export PATH=$HOME/.npm-global/bin:$HOME/.local/bin:$PATH
          printf "%-10s %s\n" node     "$(node -v 2>&1 || echo MISSING)"
          printf "%-10s %s\n" npm      "$(npm -v 2>&1 || echo MISSING)"
          printf "%-10s %s\n" codex    "$(codex --version 2>&1 | head -1 || echo MISSING)"
          printf "%-10s %s\n" claude   "$(claude --version 2>&1 | head -1 || echo MISSING)"
          printf "%-10s %s\n" opencode "$(opencode --version 2>&1 | head -1 || echo MISSING)"
          printf "%-10s %s\n" gemini   "$(gemini --version 2>&1 | head -1 || echo MISSING)"
          printf "%-10s %s\n" copilot  "$(copilot --version 2>&1 | head -1 || echo MISSING)"
          printf "%-10s %s\n" hermes   "$(hermes --version 2>&1 | head -1 || echo MISSING)"
        '
      register: verify
      changed_when: false

    - debug: var=verify.stdout_lines
